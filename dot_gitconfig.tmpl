[user]
  name = {{ .git.name }}
  email = {{ .git.email }}
[alias]
  st = status
  ch = checkout
  chb = checkout -b
  ps = push
  pl = pull
  ft = fetch
  cm = commit
  br = branch
[color]
  ui = auto
  diff = auto
  grep = auto
  status = auto
  branch = auto
  interactive = auto
{{- if eq .chezmoi.os "darwin" }}
[url "git@github.com:"]
  insteadOf = https://github.com/
[credential]
  helper = osxkeychain
{{- end }}
[push]
  # `simple` (git >= 2.0 default) refuses to push when the upstream branch
  # name differs from the current branch, preventing accidental pushes when a
  # topic branch was created with an unexpected upstream.
  default = simple
