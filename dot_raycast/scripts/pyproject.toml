[project]
name = "raycast-scripts"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "playwright>=1.40.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
